{
  "name": "sequenced-promise",
  "version": "1.0.0",
  "description": "wrapper that makes an asynchronous service run sequentially in the order it was called",
  "main": "dist/index",
  "scripts": {
    "test": "npm run prepublish && mocha index.spec.js",
    "prepublish": "babel index.js --source-maps --out-dir dist"
  },
  "keywords": [
    "promise",
    "sequence",
    "concurrency",
    "async"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/babetmihai/sequenced-promise"
  },
  "author": "Mihai Babet",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-stage-0": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.8.0",
    "mocha": "^5.2.0"
  }
}
